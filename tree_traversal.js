/*----------------Breadth First Search------------------ */

//Visit every node on the same level before going down
/*
- Create a queue and a variable to store the values of nodes visited
- Place the root node in the queue
*/

BFS() {
    var node = this.root,
    data = [],
    queue = []

    queue.push(node)
    while(queue.length) {
        node = queue.shift()
    }
}





























